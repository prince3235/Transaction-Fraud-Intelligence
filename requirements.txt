# ─────────────────────────────────────────────────────────────────────────────
# Transaction Fraud Intelligence Platform — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Versions are pinned with floor + ceiling (>=X,<Y) for reproducibility.
# Update ceilings deliberately when a major version is vetted.
# Generated: 2026-07-14

# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.103.1,<1.0
uvicorn[standard]>=0.23.2,<1.0
streamlit>=1.26.0,<2.0

# ── ML / data science ────────────────────────────────────────────────────────
scikit-learn>=1.3.0,<2.0
numpy>=1.25.2,<3.0
pandas>=2.1.0,<3.0
joblib>=1.3.2,<2.0
mlflow>=2.11.3,<4.0
shap>=0.44.0,<1.0
plotly>=5.16.1,<6.0

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.21,<3.0
alembic>=1.12.0,<2.0
psycopg2-binary>=2.9.9,<3.0

# ── LLM ──────────────────────────────────────────────────────────────────────
anthropic>=0.18.1,<1.0

# ── Scheduling ───────────────────────────────────────────────────────────────
APScheduler>=3.10.4,<4.0

# ── Rules engine ─────────────────────────────────────────────────────────────
simpleeval==0.9.13

# ── Excel export ─────────────────────────────────────────────────────────────
openpyxl>=3.1.2,<4.0

# ── Security ─────────────────────────────────────────────────────────────────
bcrypt>=4.1.2,<5.0

# ── Dev / test (consider moving to requirements-dev.txt in future) ──────────
pytest>=7.4.3,<9.0
testcontainers[postgres]>=4.0,<5.0
httpx>=0.25.0,<1.0
